# Spring Boot MVC based REST API

Setup with a Dynamic AdviceTrait mapping configuration. The `com.example.demoservlet.problem.ProblemMappingControllerAdvice` could be implemented to map exceptions dynamically to Problem definitions.
